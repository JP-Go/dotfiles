#vim: ft=bash

if [[ -f /usr/bin/exa ]]; then
    alias ls="exa --icons" 
    alias la="exa -lah --icons"
    alias ll="exa -lh --icons"
    alias l="exa -lh --icons"
fi
if [[ -f /usr/bin/nvim ]]; then
    alias vim="nvim"
    alias v="nvim"
fi

if [[ -f /usr/bin/gs ]]; then 
    alias gita="git add"
    alias gitc="git commit"
    alias gitd="git diff"
    alias gits="git status"
    alias gitp="git push"
else
    alias ga="git add"
    alias gc="git commit"
    alias gd="git diff"
    alias gs="git status"
    alias gp="git push"
fi

if [[ -f /usr/bin/lazygit ]]; then alias lg="lazygit" ; fi

alias tlmgr="$TEXMFDIST/scripts/texlive/tlmgr.pl --usermode"
alias goto=". ~/.dotfiles/scripts/goto.sh"
alias cht.sh=". ~/.dotfiles/scripts/cht.sh"
